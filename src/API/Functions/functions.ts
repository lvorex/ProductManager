import { arasql } from "./mysql";
import { checkStock } from "./checkStock";
import { injectionCheck } from "./injectionCheck";

export const functions = {
    arasql,
    checkStock,
    injectionCheck
}